{
  "name": "lab-drop",
  "version": "0.1.3",
  "description": "A homelab dashboard with a free-2D drag-and-drop layout and hardened auth — a fork of lab-dash by Anthony Gress",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "concurrently \"cd backend && npm run build\" \"cd frontend && npm run build\"",
    "preinstall": "cd ./backend && npm install && cd .. && cd ./frontend && npm install",
    "docker:build:dev": "docker build . --no-cache -t lab-drop:latest",
    "docker:build": "docker build --platform linux/amd64 . -t lab-drop:latest",
    "docker:build:multi": "docker buildx build --platform linux/amd64,linux/arm64 . -t lab-drop:latest",
    "docker:run": "docker run --platform=linux/amd64 -p 2022:80 -p 5000:5000 lab-drop:latest",
    "docker:run:dev": "docker run -p 2022:2022 lab-drop:latest",
    "clean": "rimraf node_modules package-lock.json frontend/node_modules frontend/package-lock.json backend/node_modules backend/package-lock.json",
    "lint": "concurrently \"cd frontend && npm run lint\" \"cd backend && npm run lint\""
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^5.0.5"
  }
}
